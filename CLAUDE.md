# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Static website for **CHASExcellence**, a 501(c)(3) nonprofit (EIN 42-1909009) that awards scholarships to young athletes in memory of Chase Culp #25 (Colorado Springs, Dec 21, 2011 – Aug 30, 2024). The scholarship is open to **any sport**, not just basketball (broadened from basketball-only in July 2026) — Chase himself played both basketball (Eagles) and lacrosse (Pikes Peak Lacrosse). There is no build system, package manager, framework, or test suite — just plain HTML, CSS, and vanilla JavaScript.

## Hosting & Deployment

- The repo lives in the **`chasexcellence` GitHub organization** (`github.com/chasexcellence/chasexcellence`), owned by the nonprofit.
- Hosted on **GitHub Pages**, served from the root of the `main` branch. Pushing to `main` deploys the live site automatically — there is no separate build or deploy step.
- The custom domain is **chasexcellence25.org**, defined in the `CNAME` file. DNS is managed at **GoDaddy** (outside this repo). Do not delete, rename, or edit `CNAME` unless the domain itself is intentionally changing — GitHub Pages reads this file to bind the domain, and past commits show the domain has been broken before by editing it.
- Spelling gotcha: the brand is written **CHASExcellence** (CHASE + xcellence, sharing the E), but the domain is `chasexcellence25.org`. Git history contains several commits fixing mix-ups between these spellings.
- **GitHub Pages throttles to roughly 10 builds/hour.** If a push doesn't appear live after a few minutes, an empty commit (`git commit --allow-empty -m "Trigger Pages rebuild"`) usually un-sticks a dropped build.

## Domain & Email

DNS for `chasexcellence25.org` lives at GoDaddy and serves two independent systems — be careful not to break one while editing the other:

- **Website (GitHub Pages):** four A records on the apex (`185.199.108.153` through `185.199.111.153`) and a `www` CNAME pointing to `chasexcellence.github.io`. These must stay.
- **Email (Google Workspace):** organization email (`name@chasexcellence25.org`) is hosted on Google Workspace. An MX record (`smtp.google.com`, priority 1) and a `google-site-verification` TXT record support it. Email is managed in the Google Admin Console, not in this repo or at GoDaddy beyond those DNS records.

Nothing in this repo affects email, and no DNS change is needed for normal website updates.

## Working with Claude

- When the user asks for a change, commit it and push directly to `main` without asking for confirmation first — this repo deploys automatically via GitHub Pages, and the user has explicitly opted into that.
- **After making any change to the site (content, structure, styling, new pages, integrations, etc.), update this CLAUDE.md file in the same commit** so it stays an accurate, current record of the site for any developer or future Claude session — not just a historical snapshot. Add/edit the relevant section below; don't let it drift out of sync with the code.
- Treat changes that touch real money (Venmo/Cash App handles, donation QR codes) or real third-party accounts (Formspree, social links) with extra care — confirm exact spelling/handles with the user before publishing, since a wrong QR code or link has real-world consequences.

## Local Development

Open `index.html` directly in a browser, or serve locally:

```bash
python3 -m http.server 8000
```

There are no lint, build, or test commands.

## Structure

- `index.html` — the main single page. Anchor-linked sections in order: `hero`, `about`, `scholarship` (includes the four Core Values — see below), `recipients`, `nominate`, `events`, `impact`, `donate`, `gallery`, `sponsors`, plus a closing `legacy` CTA band and the footer. Each `<section id="...">` corresponds to a nav link; adding/renaming a section means updating the nav in the `<header>` and the scroll-spy (`script.js`) picks it up automatically via `section[id]`.
- `apply.html` — a second real page: the on-site scholarship application (see Content Notes).
- `style.css` — all styling for both pages. Sections alternate backgrounds via `section-dark` / `section-green-light` classes. **CSS variable names in `:root` are legacy and no longer describe their colors** — `--green-dark` and `--gold` etc. actually hold the Carolina Blue palette (`--green-dark: #4B9CD3`, `--navy: #0A2F5C`, `--gold: #FFFFFF`). Use the existing variables rather than hardcoding colors, but don't be misled by their names.
- `script.js` — mobile nav toggle, scroll-spy nav highlighting, Tier-1 cinematic motion (scroll reveals, count-up stats, gallery photo fade-in-on-load), and the gallery lightbox. All motion respects `prefers-reduced-motion`.
- `images/` — all photos, logos, and QR codes, committed directly to the repo. Notable ones: `logo-badge.png` (transparent CHASExcellence badge, used in nav/footer/apply/favicons), `hoop-dreams-logo.png` (partner logo), `venmo-qr.png` / `cashapp-qr.png` (donation QR codes — regenerate with Python `qrcode` if the handle changes), `nominate-qr.png` (encodes `apply.html`), `tourney-flyer-2025.jpg` / `tourney-flyer-2026.jpg` (tournament flyers shown on the Events cards), `tourney-2025-trophy.jpg` (cover photo for the past tournament's Facebook slideshow card), `kickoff-flyer-2026.jpg` (Kickoff event flyer, 1254×1952 — shown on the Kickoff card, also the target of its "Download Flyer" button and the tap-to-enlarge link, so all three references share this one file). **Replaced Aug 2026 with the newer "Flyer2" design** (basketball-court background, "PRESENTS", Chase's full name and dates, gold date type), superseding the older navy/brush-stroke artwork that used to live here — the brush-stroke version and its ribbon-removal history are now only in git history. The master lives OUTSIDE this repo at `~/Downloads/Flyer2-with-sponsors-PRICESTRIP.png`, alongside its Instagram companions `Flyer2-IG-1.png` (hero slide) and `Flyer2-IG-2.png` (details + sponsors slide); `*-BEFORE-revival.png` backups sit beside them. Edit the master there and re-export to this JPG (quality 92 ≈ 600KB) — the QR and every partner logo are baked in as flat pixels, so any sponsor or URL change means regenerating the master and re-exporting, and the two IG files must be updated in parallel to stay in sync. Sponsor block reads DPORS / The Coop / Revival Roofing, then Lux & Lav / Aria / Spear D, then Colorado Sports / Tiffany. **Re-exported Aug 27 2026 to drop the expired "TICKETS BY AUGUST 23RD" clause** — the price strip now reads `$25 PER PERSON` alone. The master had already been corrected; the repo copy was just an older export, so the fix was a straight re-export (`quality=92, optimize=True`, no `subsampling` override → 628KB; passing `subsampling=0` inflates it to 766KB), not a pixel edit. **If the live JPG and the master ever disagree again, diff them before editing — the master is usually the newer one.** The baked-in ticket QR still decodes to `givebutter.com/CHASExcellence-Kickoff-Fundraiser`; always decode-verify that QR after any re-export, since it is flat pixels with no fallback if it breaks. `givebutter-general-qr.jpg` (general donation QR, Donate section), `new-35.jpg` through `new-43.jpg` (memorial/tribute photos added to the gallery — memorial cross, roadside vigil, family gatherings, the "CHASE EXCEL..." tribute banner — as opposed to the `new-01` through `new-34` range and other named `gallery-*.jpg` files, which are all candid photos of Chase's life; `new-38.jpg` is a professional family portrait bearing a "Jessica Bingham Creations" watermark, used here as a personal/family photo — reuse elsewhere would need the photographer's permission), `givebutter-icon.png` (Givebutter's official mascot icon, used on both Givebutter buttons). `kickoff-givebutter-qr.png` is the source QR for the Kickoff campaign (`givebutter.com/CHASExcellence-Kickoff-Fundraiser`) — no longer a standalone `<img>` on the page, but still referenced when regenerating the flyer footer above.

## Design

- Palette: Carolina Blue dominant surface, navy text/accents, white accents (see CSS variable gotcha above). Headings in Fredoka (weight 600), body in Nunito.
- The **Scholarship section** (`#scholarship`) merges what used to be a separate "Core Values" section: it opens with an intro naming Composure, Dedication, Toughness, and Unity (bolded navy via `.value-highlight`), then the four value cards, a "How It Works" divider, the application info cards, and the Apply CTA. There is no separate `#values` section anymore — "Values" was removed from the nav.
- Motion: scroll-reveal fade/slide-ups, hero Ken Burns zoom, count-up Impact stats, gallery photos fade in on image load (not just on scroll) since lazy-loading otherwise defeats the reveal timing.

## Content Notes

- The scholarship application lives on-site at `apply.html`, open to **any sport** (not just basketball) — may be submitted by the athlete directly or by a coach on their behalf. It POSTs to Formspree (endpoint in the file; Formspree account login is **admin@chasexcellence25.org**, free tier 50 submissions/month). "Nominate a Player" and "Apply for Scholarship" both lead there — they are the same flow. An old unlisted Microsoft Form still exists as a backup.
- `images/nominate-qr.png` encodes `https://chasexcellence25.org/apply.html`.
- Donations go through **Givebutter**, split into two distinct campaigns — don't merge them:
  - **General donations** (Donate section `#donate` card, and the header nav "Donate" button, which now links straight out instead of anchor-scrolling to `#donate`): `givebutter.com/chasexcellence-general-donation-avlssz`, QR is `images/givebutter-general-qr.jpg`.
  - **Kickoff event tickets/donations** (the Kickoff event card's "Buy Tickets / Donate" button only): `givebutter.com/CHASExcellence-Kickoff-Fundraiser`. Keep these two links distinct — the Kickoff card must not be pointed at the general campaign or vice versa. **As of the event passing, the Kickoff card's ticket/donate CTAs were removed entirely** (no more "Buy Tickets / Donate" button, no "Get Directions", no "Download Flyer", no secondary "Buy Tickets" link under the flyer, no "Can't attend? donate here" line) — the card now just shows date/location/flyer image like the completed tournament card. The Kickoff Givebutter URL/QR above are effectively orphaned on-page now (still referenced in the flyer JPG and CLAUDE.md history) but no longer linked from any live button.
  - Both buttons use `images/givebutter-icon.png` (their official mascot icon) via the shared `.btn-givebutter` class.
  - Venmo and Cash App were removed July 2026; `images/venmo-qr.png` and `images/cashapp-qr.png` are unused leftovers, not deleted from the repo.
- **Merch link** in the header nav: `https://colosports.chipply.com/CHASExcllence25/?action=viewall` (opens in new tab). Chipply storefront for CHASExcellence merchandise.
- **Raffle link** in the header nav (`#navRaffle`, between Merch and Donate): `https://givebutter.com/raffle-gift-bag-ewvz7d`, matching the Gift Bag Raffle event card. **Self-removes client-side after the raffle ends** — `script.js` compares `new Date()` against Aug 31, 2026 23:59:59 (the visitor's local device clock) and calls `.remove()` on `#navRaffle` if past it. This is a one-way, one-time check with no backend, so: (1) it relies on the visitor's device clock being roughly correct — not guaranteed, but fine for a low-stakes nav link; (2) if the raffle end date ever changes, update the `Date(2026, 7, 31, 23, 59, 59)` call in `script.js` (note JS months are 0-indexed — 7 means August) — nothing else references this date; (3) once removed it's gone for that pageview, no way to bring it back short of editing the code, so don't reuse this pattern for anything that might need to reappear.
- Social links live in the footer's "Social Media" column: Facebook (`facebook.com/profile.php?id=61591746670881`) and Instagram (`@chasexcellence25_`).
- Footer/Donate section also disclose 501(c)(3) status and EIN 42-1909009 for donor tax-deductibility.
- Fonts (Fredoka for headings, Nunito for body) load from Google Fonts; everything else is self-contained.
- **Sponsors section** (`#sponsors`, after Gallery): a `.sponsors-grid` of `.sponsor-card` logos using `grid-template-columns: repeat(auto-fit, minmax(160px, 1fr))`, so it wraps cleanly at any sponsor count without needing responsive breakpoint overrides. Logos sit directly on the section background (no white card box) — some source logo files carry their own solid-color background (e.g. `sponsor-chicken-coop.jpg`, `sponsor-aria-architecture.jpg`), which is intentional/expected, not a bug. Current sponsors, in grid order: The Chicken Coop, DPORS, Revival Roofing, Lux & Lav Aesthetics, Spear D Construction, Aria Architecture, Colorado Sports, The Tiffany Home Team (`images/sponsor-*.jpg`/`.png`). **Lux & Lav** ships as a flat near-white wordmark on transparent, which washes out on the Carolina Blue section, so the site uses `sponsor-lux-lav-card.png` — the logo pre-composited on a **black** rounded card, the dark-ground counterpart to the white cards Spear D/Colorado get. The untouched transparent original is kept as `sponsor-lux-lav.png` for flyer work. `.sponsor-card-lux` spans 2 columns (its ~2.6:1 card is width-constrained in one). **The grid uses `minmax(150px, 1fr)` rather than 160px specifically so a 6th column fits at desktop width** — that lets row 1 hold Coop + DPORS + Revival(2) + Lux(2) exactly with no trailing hole. Raising it back to 160px drops the grid to 5 columns and pushes Lux to the next row, leaving a gap; re-check that sum whenever a sponsor is added or a span changes. **Revival Roofing is the largest sponsor**, listed 3rd (after The Chicken Coop and DPORS) so it sits mid-way along the top row. Sizing rule of thumb here: a logo's rendered size is driven by its **aspect ratio**, not just card padding — a wide mark in a single ~200px column is width-constrained and ends up tiny no matter how little padding it has. So the wide logos get `grid-column: span 2` to reach a normal height: `.sponsor-card-revival` (~2:1) spans 2 and uses the default card, landing at 128px tall — the same height as The Chicken Coop; `.sponsor-card-tiffany` (~6.5:1) spans 2 with `max-width: 62%`, landing at ~35px tall (vs ~29px capped in a single column at zero padding). **Gotcha:** these per-logo `img` caps must be declared AFTER the base `.sponsor-card img` rule — both are specificity (0,1,1), so source order decides, and a cap placed above it silently does nothing. `.sponsor-card-dpors` just trims padding (it's roughly square, so it isn't width-constrained); `.sponsor-card-speard` and `.sponsor-card-colorado` likewise trim padding to `0.3rem` — for wide logos already boxed in a pre-composited card, padding is the only lever that grows them without changing the column count and disturbing row 1. **Spear D and Colorado Sports use pre-composited white-card images** — `sponsor-speard-construction-card.png` and `sponsor-colorado-sports-card.png` — rather than a CSS background, so the site matches the tight white cards those two already have on the Kickoff flyer. Both are generated from the transparent originals (`sponsor-speard-construction.png`, `sponsor-colorado-sports.png`, which are kept because the flyer build still uses them) by scaling onto a rounded white rect at 260px tall with ~11% padding and a ~14% corner radius. Spear D needs the white backing because its wordmark is dark navy on transparent and vanishes against the Carolina Blue section. Note the supplied `sponsor-speard-construction.png` is cropped flush to the ink on every edge, shaving ~1px off the round extremes of the C/O — sub-pixel at display size, but it's why the generated card adds padding instead of sitting tight to the ink. Per-sponsor tweaks live in `.sponsor-card-<name>` classes. `images/sponsor-tlc-plumbing.png` is an unused leftover from when TLC Plumbing was a sponsor, not deleted from the repo.
- **Kickoff runs 2:00 PM – 8:30 PM**, corrected Aug 27 2026 from a stale `8:00 PM` that appeared twice on the Kickoff card (the `.event-date` line and the `.event-details` list). It ends at 8:30 because Papi performs 5:30–8:30 outside. Change both occurrences together if it moves again.
- The Events section (`#events`) currently has all three cards marked `Completed` — a `.event-badge` is plain text, not a fixed enum, so it can say anything (`Ongoing`, `Upcoming`, `Completed`).
  - **Gift Bag Raffle**: ticket sales ran through Sunday Aug 30, 2026 ($5/ticket, was `givebutter.com/raffle-gift-bag-ewvz7d`); winner is **Janna Jacobs**, announced Monday Aug 31 — the card names her in the description, the detail bullet ("Winner: Janna Jacobs"), and the date line ("Winner announced Monday, August 31, 2026"). Its CTA button and QR (`images/raffle-giftbag-qr.png`, now unused but kept in the repo) were removed. The card is back to a two-column `.event-content` (not `.event-content-single`) because it now has something to show: `images/raffle-winner-flyer.jpg` (1080×1080, the Instagram winner-announcement graphic — navy/Carolina Blue brand, confetti, CHASExcellence badge logo) sits in `.event-visual` with the standard tap-to-enlarge treatment, same pattern as the Kickoff/Tournament flyers. That JPG's source is an HTML/CSS file rendered via headless Chrome (`chrome --headless --screenshot`, since no wkhtmltoimage/Playwright are installed) — not built in this repo, so if the winner name or copy ever needs to change, there's no in-repo source to edit; it'd need to be regenerated from scratch or asked for again. The header nav's `#navRaffle` button (see above) was NOT touched when this card went Completed — it still self-removes at the Aug 31 23:59:59 boundary, independent of this card's status.
  - **The CHASExcellence Foundation Kickoff** and the **3v3 Tournament**: both keep their flyer image in `.event-visual` (two-column `.event-content`), just had their CTA buttons removed (Buy Tickets/Donate, Get Directions, Download Flyer, and for Kickoff also the secondary "Buy Tickets" link under the flyer and the "Can't attend?" donate line).
- The upcoming 3v3 Tournament card has an `.event-media` block (below its `.event-content`) with a Facebook video: `images/tourney-2026-team.jpg` (the Eagles team + coach with the CHASExcellence banner, from `~/Downloads/A7V00006.JPG`) as the `.facebook-reel-card` background, linking to `https://www.facebook.com/share/v/1FpgUqPhRy/`. **Do not reuse `images/tourney-2025-trophy.jpg` for anything 2026-dated** — that photo has "2025 CHAMPIONS" engraved on the trophy plate, baked into the image itself. This card uses `fb-reel-overlay fb-reel-overlay-strong` (not the plain `.fb-reel-overlay` the past-event reel card uses) because its background photo has a large near-white banner with QR codes right where the text row sits — the default gradient (82% black at the very bottom, fading out by mid-card) isn't dark enough against near-white; `-strong` is a near-solid ~97% band starting further up. Reach for `-strong` on any future reel-card background that's light/busy near the bottom edge; keep the default for darker photos like the 2025 trophy shot. Fixing this card's contrast surfaced a real bug in `.facebook-reel-card` shared by **both** reel cards: at mobile width (~245px card), `.fb-reel-title`/`.fb-reel-sub` would wrap instead of truncating, inflating `.fb-reel-row`'s height to ~95% of the card and pushing washed-out text up over the photo. Fixed with `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` on both (base styles), plus a `max-width: 600px` block that hides `.fb-reel-sub` entirely and shrinks the icon/padding, since even a single truncated line of both title+subtitle still crowds a ~140px-tall card. Verify any future reel-card content at mobile width, not just desktop — this one looked perfect on desktop and was badly broken on mobile.
- The Kickoff card's flyer thumbnail has a "Buy Tickets →" button (`.event-flyer-buy-link`) below the "Tap to view the flyer" caption, linking straight to the Givebutter campaign. An earlier version floated this as an absolutely-positioned pill overlaid on top of the flyer image, but at the thumbnail's small render size (200px wide on desktop) there was no spot big enough to fit a legible button without covering either the QR code or a sponsor logo baked into the graphic — a normal in-flow link below the image avoids that entirely.
